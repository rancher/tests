#Required for the template test
templateTest:
  repo:
    metadata:
      name: "test"
    spec:
      gitRepo: "<HB_TEMPLATE_GIT_REPO>"
      gitBranch: "main"
      insecureSkipTLSVerify: true
  templateProvider: "<HB_PROVIDER>"
  templateName: "rke2-clusterTemplate1"
