# Dockerfile for Airgap Go Testing
# Provides Go toolchain, gotestsum, OpenTofu, Ansible, and AWS CLI for test execution

ARG TOFU_VERSION=1.10.7
ARG GOTESTSUM_VERSION=v1.13.0
ARG ANSIBLE_CORE_VERSION=2.21.1
ARG AWSCLI_VERSION=1.44.44

FROM --platform=linux/amd64 golang:1.27-alpine3.23@sha256:0908ac9b9319e09d7c238aabe914e0395c51d63c4e3d0ae8c554fda9158a5769

ARG TOFU_VERSION
ARG GOTESTSUM_VERSION
ARG ANSIBLE_CORE_VERSION
ARG AWSCLI_VERSION

# Allow go to download a newer toolchain when go.mod requires one (official
# golang images default to GOTOOLCHAIN=local, which hard-fails instead). Keeps
# pipelines building while the base image digest lags a go.mod bump in
# rancher/rancher main. Only downloads when the module requires it.
ENV GOTOOLCHAIN=auto

# Install runtime dependencies
RUN apk add --no-cache \
    bash \
    curl \
    git \
    make \
    openssh-client \
    python3 \
    py3-pip \
    ca-certificates \
    && ln -sf /bin/bash /usr/bin/bash \
    && rm -rf /var/cache/apk/*

# Install Ansible core and AWS CLI (pinned versions for reproducible builds).
# ansible-core and the python deps mirror qa-infra-automation/requirements.txt so
# the container control node matches the locally validated environment.
RUN pip3 install --no-cache-dir --break-system-packages \
    ansible-core==${ANSIBLE_CORE_VERSION} \
    awscli==${AWSCLI_VERSION} \
    pyyaml==6.0.2 \
    kubernetes>=29.0.0 \
    boto3>=1.34.0 \
    botocore>=1.34.0

# Ansible collections required by the qa-infra-automation playbooks.
# Versions mirror qa-infra-automation/requirements.yml (kept in sync manually:
# the rancher/tests build context does not contain that repository).
RUN ansible-galaxy collection install \
    kubernetes.core:5.0.0 \
    community.general:10.1.0 \
    cloud.terraform:2.2.0 \
    ansible.posix:1.6.2 \
    amazon.aws:8.2.0

# Install OpenTofu with checksum verification
RUN curl -Lo /tmp/tofu_${TOFU_VERSION}_linux_amd64.tar.gz "https://github.com/opentofu/opentofu/releases/download/v${TOFU_VERSION}/tofu_${TOFU_VERSION}_linux_amd64.tar.gz" \
    && curl -Lo /tmp/tofu_SHA256SUMS "https://github.com/opentofu/opentofu/releases/download/v${TOFU_VERSION}/tofu_${TOFU_VERSION}_SHA256SUMS" \
    && cd /tmp && grep "tofu_${TOFU_VERSION}_linux_amd64.tar.gz$" tofu_SHA256SUMS > /tmp/tofu.sha256 \
    && test -s /tmp/tofu.sha256 \
    && sha256sum -c /tmp/tofu.sha256 \
    && rm /tmp/tofu.sha256 \
    && tar -xzf /tmp/tofu_${TOFU_VERSION}_linux_amd64.tar.gz -C /usr/local/bin/ \
    && rm /tmp/tofu_${TOFU_VERSION}_linux_amd64.tar.gz /tmp/tofu_SHA256SUMS \
    && chmod +x /usr/local/bin/tofu

# Install gotestsum (pinned version for reproducible builds)
ENV GOBIN=/usr/local/bin
RUN go install gotest.tools/gotestsum@${GOTESTSUM_VERSION}
ENV GOBIN=

# Set Ansible config to the consolidated ansible.cfg in qa-infra-automation
ENV ANSIBLE_CONFIG=/workspace/qa-infra-automation/ansible/ansible.cfg

# Create working directory
WORKDIR /workspace

# Verify installations
RUN tofu version && \
    ansible --version && \
    aws --version && \
    go version && \
    gotestsum --version

# Default to sh shell (compatible with docker run sh -c)
CMD ["/bin/sh"]
