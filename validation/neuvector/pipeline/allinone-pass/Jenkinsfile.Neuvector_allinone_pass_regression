#!groovy
node('neuvector') {
    stage('Execution') {
   withCredentials([string(credentialsId: 'NEUVECTOR_PASSWORD', variable: 'NEUVECTOR_PASSWORD')]) {
    sh '''
      sshpass -p "$NEUVECTOR_PASSWORD" ssh neuvector@10.1.5.209 "/mnt/nfs/share/QA/automation/allinone_regression.sh $_BUILD_TAG_ $_DIR_"
    '''
   }
}
}
