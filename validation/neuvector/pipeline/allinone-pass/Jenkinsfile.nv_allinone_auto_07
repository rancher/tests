#!/groovy
node('neuvector') {
    stage('Execution') {
   withCredentials([string(credentialsId: 'NEUVECTOR_PASSWORD', variable: 'NEUVECTOR_PASSWORD')]) {
    sh '''
      sshpass -p "$NEUVECTOR_PASSWORD" ssh neuvector@10.1.5.65 "/mnt/nfs/share/QA/automation/allinone_script.sh $NV_RELEASE_TAG $_DIR_ $JOB_NAME $BUILD_NUMBER"
    '''
   }
}
}
