#!/groovy
node('neuvector') {
    stage('Execution') {
   withCredentials([string(credentialsId: 'NEUVECTOR_PASSWORD', variable: 'NEUVECTOR_PASSWORD'), string(credentialsId: 'NEUVECTOR_K8S_SERVER_12', variable: 'NEUVECTOR_K8S_SERVER_12'), string(credentialsId: 'NEUVECTOR_K8S_OC_USERNAME', variable: 'NEUVECTOR_K8S_OC_USERNAME')]) {
    sh '''
      ssh -o "StrictHostKeyChecking no" -i /$NEUVECTOR_K8S_OC_USERNAME/.ssh/id_rsa $NEUVECTOR_K8S_OC_USERNAME@$NEUVECTOR_K8S_SERVER_12 "/mnt/nfs/share/QA/automation/k8s_script.sh $_JOB_TYPE_ $_BUILD_TAG_ $_DIR_"
    '''
  }
 }
}
