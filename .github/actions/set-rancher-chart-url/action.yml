---
name: "Set Rancher Chart URL"
description: "Sets the Rancher url depending on the provided Rancher repo"
inputs:
  rancher-repo:
    description: "The rancher repo"
    required: true
  is-prime:
    description: "Determines if the Rancher is Prime or Community"
    required: true
  community-chart-url:
    description: "The Community chart url to use if the repo is latest or alpha"
    required: true
  staging-chart-url:
    description: "The Prime chart url to use if the repo is latest or alpha"
    required: true
  fallback-chart-url:
    description: "The fallback chart url if the repo is not latest or alpha"
    default: ""
  env-var-name:
    description: "The name of the environment variable to set"
    default: "RANCHER_HELM_CHART_URL"
outputs:
  chart_url:
    description: "The determined chart url"
    value: ${{ steps.determine-chart-url.outputs.chart_url }}
runs:
  using: "composite"
  steps:
    - name: Determine chart url
      id: determine-chart-url
      shell: bash
      run: |
        RANCHER_REPO="${{ inputs.rancher-repo }}"
        IS_PRIME="${{ inputs.is-prime }}"

        # Register all possible chart URLs for masking before selection.
        echo "::add-mask::${{ inputs.community-chart-url }}"
        echo "::add-mask::${{ inputs.staging-chart-url }}"
        echo "::add-mask::${{ inputs.fallback-chart-url }}"

        if [[ "$IS_PRIME" == "true" ]]; then
          if [[ "$RANCHER_REPO" == "latest" ]] || [[ "$RANCHER_REPO" == "alpha" ]]; then
            CHART_URL="${{ inputs.staging-chart-url }}"
            echo "Selected chart URL source: staging for repo=$RANCHER_REPO"
          else
            CHART_URL="${{ inputs.fallback-chart-url }}"
            echo "Selected chart URL source: fallback for repo=$RANCHER_REPO"
          fi
        elif [[ "$IS_PRIME" == "false" ]]; then
          if [[ "$RANCHER_REPO" == "latest" ]] || [[ "$RANCHER_REPO" == "alpha" ]]; then
            CHART_URL="${{ inputs.community-chart-url }}"
            echo "Selected chart URL source: community for repo=$RANCHER_REPO"
          else
            CHART_URL="${{ inputs.fallback-chart-url }}"
            echo "Selected chart URL source: fallback for repo=$RANCHER_REPO"
          fi
        fi

        echo "::add-mask::$CHART_URL"

        echo "chart_url=$CHART_URL" >> $GITHUB_OUTPUT
    
    - name: Set environment variable
      uses: ./.github/actions/set-env-var
      with:
        key: ${{ inputs.env-var-name }}
        value: ${{ steps.determine-chart-url.outputs.chart_url }}